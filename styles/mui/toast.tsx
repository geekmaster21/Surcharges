import { makeStyles } from "@material-ui/core";

export default makeStyles(() => ({
  toast: {
    flexGrow: 0,
    textAlign: "center",
    color: "var(--white-1)",
    backgroundColor: "var(--grey-4)",
  },
}));
